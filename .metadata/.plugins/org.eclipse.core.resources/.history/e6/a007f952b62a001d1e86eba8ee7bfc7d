/**
 * 给ztree生成的树形结构，更换图标
 */
// treeId 树形结构的根结点
// treeNode 当前正在生成的节点
function myAddDom(treeId, treeNode) {
	console.log(treeId)
	console.log(treeNode)
	//class="glyphicon-check"
	var spanId = treeNode.tId + "_ico"// 获取ztree的图标id

	$("#" + spanId)
		.removeClass()//移除默认的class
		.addClass(treeNode.icon)//更换为数据库的图标
}
//鼠标移入显示操作图标
function myAddHoverDom(treeId, treeNode) {
	var btnGroupId = treeNode.tId + "_btnGrp"//a标签的父节点
	var aId = treeNode.tId + "_a"// 获取树形菜单的a标签id
	var span = `
		<span id=${btnGroupId}>
			<a class="btn btn-info dropdown-toggle btn-xs" style="margin-left:10px;padding-top:0px;" href="#" title="修改权限信息">&nbsp;&nbsp;
				<i class="fa fa-fw fa-edit rbg "></i>
			</a>
			<a class="btn btn-info dropdown-toggle btn-xs" style="margin-left:10px;padding-top:0px;" href="#">&nbsp;&nbsp;
				<i class="fa fa-fw fa-times rbg "></i>
			</a>
		</span>
	`
	$("#" + aId).after(span)
}
//鼠标移除隐藏操作图标
function myRemoveHoverDom(treeId, treeNode) {
	
	var btnGroupId = treeNode.tId + "_btnGrp"//a标签的父节点
	$("#btnGroupId").romve();//移除当前节点

}